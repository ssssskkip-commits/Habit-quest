import { Check, Dumbbell, Sparkles } from 'lucide-react'
import { PixelCard } from '../ui/PixelCard'
export function QuestCard({title,xp,done=false}:{title:string;xp:number;done?:boolean}){return <PixelCard className="flex items-center gap-3 p-3"><div className={`grid h-11 w-11 shrink-0 place-items-center border-2 ${done?'border-success bg-success/10 text-success':'border-slate-600 text-gold'}`}>{done?<Check size={22}/>:<Dumbbell size={22}/>}</div><div className="min-w-0 flex-1"><p className={`truncate font-bold ${done?'text-slate-500 line-through':'text-white'}`}>{title}</p><p className="mt-1 flex items-center gap-1 text-xs text-purple-300"><Sparkles size={12}/>+{xp} XP</p></div><div className={`h-5 w-5 border-2 ${done?'border-success bg-success':'border-slate-600'}`}/></PixelCard>}
